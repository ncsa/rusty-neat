//! Need some larger tests. I would like tests that could run something like fasta_tools::read_fasta an larger
//! file, like ecoli. Probably need more tests than that.
use serde_json;

#[test]
fn check_data() {
    // use read fasta on an ecoli. Give it a dir that you can extract how many files it ends up writing, their sizes
    // and an estimate of how much disk space and maybe memory the function uses.
    todo!()
}