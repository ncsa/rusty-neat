# Full path to reference in fasta format
reference: /home/joshfactorial/code/rusty-neat/generate_reads/src/utils/test_data/H1N1.fa
# Whole number length of reads
read_len: 150
# Whole number target coverage value 
coverage: .
# float giving the average mutation rate
mutation_rate: .

# integer value for ploidy
ploidy: .
# boolean
paired_ended: .
# floats that give the mean and standard deviation of the set
fragment_mean: .
fragment_st_dev: .

# Boolean values
produce_bam: true
produce_vcf: true
produce_fastq: true

# true or false (default false)
overwrite_output: .

# the directory where you want to write files
output_dir: .
# the common name given to all output files (e.g., "TEST" => TEST_r1.fastq, TEST.vcf, TEST.bam)
output_prefix: .

# input files (full paths to files)
mutation_model: .
fragment_madel: .
sequence_error_model: .

# debug option (default false)
debug: true
rng_seed: "2025 09 07 16 17 06 885084669"
